import React from "react";

const Header = () => (
    <header className="App-header" role="banner">
        <h1 className="App-title" id="main-heading">Demo Account and Document Preference Delivery</h1>
    </header>
);

export default Header;
